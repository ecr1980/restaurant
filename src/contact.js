export default function contact(){
  const contact = document.createElement('h2');

  contact.innerHTML="Ya want to contact us? Pull the rope outside our door for the foghorn. We'll either sail back to shore or open the door for ya.";

  return contact;
}